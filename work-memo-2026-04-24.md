# 车企联问题对齐备忘录

更新时间：2026-04-24

## 当前目标

- 逐个页面对照 `ytcar-app`，把反馈表中的 Android 端问题收口到真实状态。
- 区分 `已修复`、`已对齐`、`非问题`、`非客户端问题`，避免状态写重。
- 能本地修的直接落代码；需要账号、真实数据、服务端修复的单独标注。

## 已完成

- 整理并持续更新 [feedback-readable-summary-2026-04-23.md](/Users/source/AutoMobileUnion/feedback-readable-summary-2026-04-23.md)。
- 已完成并编译验证的对齐/修复：
  - `#10` 车辆图标拆分为“详情卡片规则”和“通用地图规则”。
  - `#17` 从业资格真题按真实 `category_list` key 解析。
  - `#27` 培训登录协议布局修正。
  - `#33/#46` 证书和继续教育证明版式继续收紧，当前仍记为“已对齐部分”。
  - `#35` 年度支付使用 `year.id`，并兼容金额类型。
  - `#45` 安全检查默认检查人/检查单位优先取 `nickname`。
  - `#50` 去掉 Android 额外增加的“进度未满 100% 禁止考试”本地拦截。
  - `#53/#56` 两类人员题库改为动态解析全部分类 key。
  - `#57` 报表分页结束条件按总页数判断。
  - `#59` 轨迹页“播放时长”已改“分享时间”，新轨迹会先重置播放状态，拖动进度条后可恢复播放。
  - `#31` 查车地图头像点击前先恢复普通登录缓存；`LoginActivity` 有 token 时优先展示登录态区域。
- `./gradlew :app:compileDebugKotlin` 已通过。

## 当前仍需继续

- 继续遍历 `已对齐部分`，把还能本地闭环的项压成真正的 `已修复`。
- 优先继续真机验证：
  - `#31` 冷启动时“全部车辆 / 头像 / 登录页展示”是否始终一致。
  - `#59` 轨迹页拖动、暂停、切速、重新选时间后的播放状态。
  - `#33/#46` 长企业名、长编号、长正文下的证书版式。

## 待数据或待服务端

- 需要账号/日志继续定位：
  - `#26` 培训账号登录 app 闪退。
  - `#32` 修改密码后重新登录闪退。
- 服务端问题：
  - 通知公告 500：当前已确认客户端已按 `ytcar-app` 对齐，请求返回服务端 SQL `id in ()` 异常，交服务端处理。

## 提交计划

- 本次把当前所有未提交代码和备忘录一起提交到 `main`。
- 提交后推送到 `origin/main`。
