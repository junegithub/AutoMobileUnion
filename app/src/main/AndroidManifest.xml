<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 地图相关权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 存储权限（报表导出） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AutoMobileUnion"
        tools:targetApi="31">

        <!-- 高德地图Key（需要替换为自己的Key） -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="61d6757b325acc419cc37ef20ee962bb"/>

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AutoMobileUnion"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".pages.LoginActivity" />
        <activity android:name=".pages.DeviceAlarmActivity" />
        <activity android:name=".pages.status.DeviceStatusActivity" />
        <activity android:name=".pages.OperationAnalysisActivity" />
        <activity android:name=".pages.ReportActivity" />
        <activity android:name=".pages.status.DeviceStatusListActivity" />
        <activity android:name=".pages.status.ExpireCarActivity" />
        <activity android:name=".pages.ReportAlarmDetailActivity" />
        <activity android:name=".pages.CarInfoActivity" />
        <activity android:name=".pages.OfflineDetailActivity" />
        <activity android:name=".pages.TreeListActivity" />
    </application>

</manifest>